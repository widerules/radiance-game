package org.anddev.game.combat;

import java.util.Vector;

import org.anddev.game.character.Character;
import org.anddev.game.items.ArmorItem;
import org.anddev.game.items.ShieldItem;

public class DefenceMethods {
	/*
	public static String do_defence(Character achar, Vector<Attack> atts) {
		StringBuilder result = new StringBuilder();
		int defence = achar.defense();
		ArmorItem armor = achar.get_armorSlot();
		ShieldItem shield = achar.get_shieldSlot();
		int totalArmor = armor.armor() + shield.armor();
		double  block = shield.blockChance() + (0.001*defence);
		
		while(!atts.isEmpty()) {
			Attack att = atts.lastElement();
			atts.remove(atts.size());
			
			
		}
		
		
		return result.toString();
	}
	*/
}

